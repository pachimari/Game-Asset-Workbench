# M6：产品化收口与可展示指标

本文件用于定义 M6 的目标：在 M5 已经跑通正式 Web 工作台基础盘之后，把系统从“能用”推进到“可演示、可开源、可作为求职作品展示”。

## 1. M6 的核心判断

M5 已经完成了这些关键事情：

- Python core + 薄 API + React Web 前端的正式分层已经建立
- 批次 / 条目 / 工作台 / Provider / 候选池 / 星标 / 导出主链路已跑通
- 主要第三方 Provider 协议已开始收敛成可扩展适配层

但 M5 仍主要解决的是“基础盘”和“主流程”，还没有完全解决：

- 状态同步与异步反馈的一致性
- 批量操作与批次级控制的成熟度
- 项目内可见、可量化的流程指标
- 面向 GitHub / 求职展示的产品完成度

因此，M6 的目标不是再扩一套新架构，而是：

1. 收稳核心交互
2. 补齐批次级指标与运营视角
3. 提升产品完成度与展示价值

## 2. M6 的目标分层

```text
web
  - 批次指标
  - 批量控制
  - 状态反馈一致性
  - UI/UX 收尾
  ↓
api
  - 批次指标接口
  - 导出与批量操作增强
  - provider 适配补强
  ↓
core
  - 状态与事件一致性
  - metrics 聚合
  - provider 探测与能力缓存
```

## 3. M6 重点方向

### 3.1 批次级指标面板

目标：

- 在项目内部直接看到流程效果，而不只是 README 里写一段说明

建议第一批指标：

- `批次总耗时`
- `条目平均耗时`
- `brief / prompt / image` 各阶段重做次数
- `候选采纳率`
- `星标数量 / 最终采纳是否来自星标`
- `失败次数与失败步骤分布`
- `Provider / 模型使用分布`

这些指标应优先在批次页顶部可见，而不是埋在日志里。

### 3.2 状态一致性收口

目标：

- 所有生成中、失败、完成、回退状态在批次页、条目页、候选池里表达一致

重点问题：

- 切换 item / 返回批次页时的异步状态不丢失
- 同步生成与异步生成都能稳定反馈
- 批量执行时外层列表与内层工作台状态一致

### 3.3 批量控制增强

目标：

- 让批次页真正像控制中心，而不是只是一张列表

建议能力：

- 批次级一键推进
- 批次级失败重试
- 顺序执行 / 保守并发切换
- 条件筛选后批量执行
- 星标结果批量导出

### 3.4 Provider 产品化

目标：

- 新增第三方站点时，尽量不再靠临时 patch

建议方向：

- provider 协议类型进一步收敛
- 模型同步与可用性探测分离
- 已验证模型缓存
- 更清楚的同步错误与调试信息

### 3.5 开源与作品包装

目标：

- 让项目不仅“能跑”，还“看起来完整、可信、值得读”

建议输出：

- README 更新为 Web 主线
- M5 / M6 阶段文档清晰分层
- 项目截图 / 演示 GIF
- 一页 case study 风格说明

## 4. M6 不做什么

M6 不做：

- 重写核心状态机
- 重新推翻 API 与前端分层
- 直接冲 SaaS 化或多人协作
- 引入大量与当前主流程无关的新功能

## 5. M6 的执行顺序

### M6.1 指标面板

先把批次级指标做出来，建立项目内可见的效果证明。

### M6.2 状态与异步反馈收稳

把“切页后状态丢失”“外层列表与内层工作台不同步”这类问题压到最低。

### M6.3 批量控制增强

补齐批次级操作与保守并发能力。

### M6.4 Provider 收口

减少站点级 patch，提升新增第三方站点时的稳定性。

### M6.5 展示与文档包装

同步 README、截图、项目说明，让项目具备开源展示价值。

## 6. M6 验收标准

M6 结束时，至少应满足：

1. 批次页可直接看到关键流程指标
2. 批量执行与批量导出具备稳定反馈
3. 工作台与批次页的状态表达保持一致
4. 第三方 Provider 接入体验明显更稳
5. README 与阶段文档能支撑 GitHub 展示与求职作品说明
