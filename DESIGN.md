# Design: AI 图标流水线 — 功能闭环 + UI 清理

Generated by /office-hours on 2026-03-20
Branch: codex/m3-streamlit-workspace
Repo: ai-icon-pipeline
Status: DRAFT
Mode: Builder

## Problem Statement

项目目前在 M3 阶段（Streamlit 工作台），核心流程能跑通 mock，但存在两个主要问题：
1. **功能不完整**：有些流程有 bug，Gemini API 尚未接入，部分操作可能走不通
2. **UI 交互粗糙**：Streamlit 界面布局杂乱，按钮位置不合理，跟 AI 迭代 UI 沟通成本高

用户的核心痛点不是"不知道做什么"，而是**跟 AI 协作迭代 UI 的效率低**——UI 是视觉/空间的，纯文字沟通天然有损耗。

## What Makes This Cool

- **Human-in-the-loop 的游戏资产生成**：不是全自动，而是 AI 生成 + 人工审核的闭环
- **涌现式产品设计**：用户依赖与 AI 协作的"涌现效应"来发现产品应该是什么样，不过度规定
- **本地可跑通的 Demo**：不依赖云服务，纯本地，适合展示和迭代

## Constraints

- **时间有限**：用户需要在工作之余推进，不是全职项目
- **AI 沟通成本**：UI 迭代比架构更难跟 AI 聊，需要更结构化的沟通方式
- **MVP 思维**：先跑通，再优化；先内部用，再开源
- **技术栈已定**：Python + Streamlit + Gemini Image API，短期内不换框架

## Premises

1. **清理功能闭环比调 UI 更优先** — agree?
2. **Streamlit 在 demo 阶段够用** — agree?
3. **先用 mock 跑通流程，再接 API** — agree?
4. **先内部使用，稳定后再开源** — agree?

用户确认：全部同意。

## Approaches Considered

### Approach A: 先修功能，再统一调 UI（推荐）
- Summary：先把所有功能跑通（bug 修掉、API 接上），最后集中精力调 UI
- Effort: 中等
- Risk: 低
- Pros: 不会在 UI 上卡住进度；功能跑通后才有底气调 UI
- Cons: 丑的状态可能持续一段时间

### Approach B: 边修功能边调 UI
- Summary：每修一个功能，就把对应页面的 UI 调到满意
- Effort: 较高
- Risk: 中等
- Pros: 每一步都是"能展示"的状态
- Cons: 跟 AI 迭代 UI 效率低，可能拖慢整体进度

### Approach C: 先画 wireframe，再让 AI 实现
- Summary：自己先画出每个页面的布局，再交给 AI 实现
- Effort: 中等（需要额外画图）
- Risk: 低
- Pros: AI 有明确的视觉参考，减少沟通成本
- Cons: 多一步设计工作

## Recommended Approach

**选择 A**：先把功能闭环修完，再统一调 UI。

理由：
- 用户的核心目标是"能跑通"，不是"好看"
- UI 迭代成本高，放在功能稳定后更高效
- 先用 mock 验证流程正确性，再接 API 时只处理 API 相关问题

**建议的补充**：在调 UI 之前，可以花 30 分钟画个简单的 wireframe，这样后续跟 AI 聊 UI 时有参考。

## Open Questions

1. 具体哪些功能有 bug？需要用户重新跑一遍 mock 流程来确认
2. Gemini API 的接入需要什么准备工作？（API key、endpoint 配置等）
3. UI 的"理想状态"是什么？（建议用户先画 wireframe 或者找参考）

## Success Criteria

- [ ] mock 流程 100% 闭环：创建任务 → 添加 item → 需求整理 → prompt 生成 → 图片生成 → 审核 → 落盘
- [ ] 每个阶段的人工审核操作（通过/重跑/手改/回退）都能正常工作
- [ ] Streamlit 界面布局合理，关键操作入口清晰
- [ ] 能给同事演示一次完整的 mock 流程

## Next Steps

### 第一步：跑 mock 流程，列出 bug 清单
```bash
PYTHONPATH=src python3 -m ai_icon_pipeline create-task \
  --task-name "测试任务" \
  --project-background "测试背景" \
  --style-requirements "测试风格"

PYTHONPATH=src python3 -m ai_icon_pipeline create-item task_001 \
  --asset-type "skill_icon" \
  --title "测试图标" \
  --description "测试描述" \
  --category "combat"

PYTHONPATH=src python3 -m ai_icon_pipeline run-pipeline task_001
```

### 第二步：启动 Streamlit，检查 UI 交互
```bash
python3 -m streamlit run src/ai_icon_pipeline/ui.py
```

### 第三步：逐个修复 bug
按优先级：流程 bug > 状态机 bug > UI bug

### 第四步：接 Gemini API
- 配置 API key
- 替换 mock 调用为真实 API 调用
- 处理 API 错误和重试

### 第五步：调 UI（等功能稳定后）
- 画 wireframe
- 让 AI 按 wireframe 实现
- 逐页面调整

## What I noticed about how you think

- **你依赖涌现，不依赖规划**："我要是全部写好，就很死板"——你更喜欢在迭代中发现方向，而不是一开始就规定死。这是很好的产品直觉。
- **你能分清"能跑"和"好用"**：你知道 MVP 能跑不代表能用，但你也知道不要在 demo 阶段追求完美。
- **你知道 AI 协作的边界**：你意识到"跟 AI 聊架构容易，聊 UI 难"，这是很真实的洞察。
- **你有务实的优先级判断**：选方案 A（先功能后 UI）而不是追求每一步都好看，说明你知道什么时候该忍，什么时候该优化。
