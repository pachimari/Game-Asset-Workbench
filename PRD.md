# AI 游戏图标资产生成流水线 - 产品需求文档 (PRD)

## 1. 执行摘要

### 问题陈述
游戏开发过程中，图标资产设计流程繁琐：需要人工整理需求、转化为图像 Prompt、协调美术资源、反复修改迭代。技能图标、Buff 图标、道具图标、货币图标等都存在类似问题。每个图标从需求提出到最终产出需要 1-2 周时间，且在迭代过程中存在大量返工。

### 解决方案
构建一套 AI 驱动的多 Agent 游戏图标资产生成流水线，通过“主控编排 + 子 Agent（需求整理、出图指令生成）+ 图像生成 API（Gemini）”实现：
"输入批量图标需求 → AI 分步产出 → 人工审核/重跑/手改 → 输出图标 + 全过程记录" 的闭环。

本 PRD 以“本地可跑通 Demo”为目标：优先让 SOP 可重复、可演示、可迭代，不追求一次性把所有智能化（例如 Meta-Agent 自动调优）做满。

### 成功指标

以下指标用于 Demo 叙事与后续优化方向，前期不作为硬性验收门槛（以 SOP 跑通为先）：

| 指标 | 建议目标值 | 说明 |
|------|------------|------|
| SOP闭环可跑通 | 100% | 表单提交→分步产出→审核→落盘→导出 |
| 任务完成时间 | 30-60分钟 | 从创建任务到通过审核（含人工） |
| 首次通过率 | 20-40% | 依赖出图模型与美术规范约束强度 |
| 可追溯性 | 100% | 每一步输入/输出/操作/时间/成本可回放 |

---

## 2. 用户体验与功能

### 2.1 用户画像

| 角色 | 特点 | 使用场景 |
|------|------|----------|
| 游戏策划 | 不懂技术，了解游戏背景和需求 | 输入批量图标需求、审核AI输出 |
| 美术对接 | 了解美术规范 | 审核图标、提出修改意见 |
| 运营人员 | 需要快速产出测试用图标 | 批量生成、筛选 |

### 2.2 用户故事

**故事1: 批量图标资产生成**
- 作为一个游戏策划，我想要提交一批游戏内图标需求，然后自动生成候选图标，这样不需要逐条等待美术排期
- 验收标准：
  - 输入批量需求后，系统按 item 自动依次执行：需求整理Agent → 出图指令生成Agent → 出图程序（Gemini Image API）
  - 每个 item 的每个环节结果可见可审核
  - 支持一个 task 内包含多个 item

**故事2: 人工审核与迭代**
- 作为一个游戏策划，我想要在每个AI生成环节审核并决定是否通过，这样可以在早期发现问题避免后续浪费
- 验收标准：
  - 需求整理Agent结果展示后，用户可选择"通过"、"重跑"、"手动修改"
  - 出图指令生成Agent结果展示后，用户可选择"通过"、"重跑"、"手动修改"
  - 图片结果展示后，用户可选择"通过"、"重跑出图"、"返回修改出图指令"
  - 所有操作记录到聊天记录

**故事3: 自然语言交互**
- 作为一个游戏策划，我想要通过自然语言告诉AI需要重做或修改某个部分，这样操作更直观
- 验收标准：
  - 用户在聊天窗口输入"重跑需求整理"、"把颜色改成蓝色"等指令
  - 主控编排层理解指令并执行相应操作
  - 支持混合使用按钮和聊天两种方式
  - MVP 阶段优先支持有限 action 集，自由聊天作为增强层存在，解析失败时可回退到按钮操作

**故事4 (vNext): Meta-Agent调优**
- 作为一个高级用户，我想要主控Agent能够分析并优化子Agent的Prompt配置，这样系统可以持续自我改进
- 验收标准：
  - 当子Agent输出持续不佳时，主控Agent可识别并修改其Prompt配置
  - 支持查看和回滚Prompt配置历史

**故事5: 量化指标追踪**
- 作为一个使用者/面试官，我想要看到任务的全维度量化指标，这样能评估系统效果并持续优化
- 验收标准：
  - 每个任务记录：耗时、迭代次数、API调用数、Token消耗、费用预估
  - 展示任务统计面板
  - 支持导出历史任务数据

### 2.3 非目标

- **不支持复杂图像编辑**：仅生成图标，不提供PS类编辑功能
- **不替代美术终稿**：产出为参考/测试用，终稿仍需美术制作
- **不支持实时协作**：单人使用，不支持多人同时编辑
- **不提供SaaS服务**：仅本地部署Demo
- **MVP不做全自动Meta-Agent**：仅提供“调优建议 + 人工确认 + 版本记录/可回滚”

---

## 3. AI系统需求

### 3.1 Agent架构

```
┌─────────────────────────────────────────────────────────────────┐
│           主控编排 = Streamlit + 本地编排代码 (Python)           │
│   - 维护任务状态机/落盘/指标                                     │
│   - 驱动每一步执行、重跑、手改、导出                             │
│   - (可选) 调用 Claude Code CLI 做意图路由/结构化输出            │
└─────────────────────────────────────────────────────────────────┘
                              ↑
              ┌───────────────┴───────────────┐
              │   用户聊天 / 表单按钮         │
              └───────────────┬───────────────┘
                              ↓
┌─────────────────────────┐  ┌─────────────────────────┐  ┌─────────────┐
│  需求整理Agent          │  │  出图指令生成Agent      │  │  出图程序   │
│  - 整理标题/描述/关键词   │  │  - 生成出图指令/Prompt  │  │  - 调用Gemini│
│  - 理解项目上下文         │  │  - 适配美术规范         │  │  - 批量生成 │
│  + Prompt模板配置       │  │  + Prompt模板配置       │  │  + 后处理(可选)│
└─────────────────────────┘  └─────────────────────────┘  └─────────────┘
```

**说明：**
- **主控编排（本地代码）**：负责任务目录、状态机、按钮动作、落盘、导出与指标采集；MVP 不依赖“一个全能 Agent”来承担编排。
- **Claude Code CLI（可选但推荐）**：用于把自然语言指令（如“重跑需求整理”“改成蓝色”）转换为结构化 action，让编排层可稳定执行；API Key 等由本机 Claude Code 管理，Demo 项目不管理。
- **需求整理Agent**：负责将粗糙的图标需求转化为适合后续出图的标题、描述、关键词和视觉重点。
- **出图指令生成Agent**：负责将文案转化为图像生成模型可理解的出图指令/Prompt，并适配美术规范。
- **出图程序**：纯代码实现，循环调用 Gemini Image API 生成图片，不涉及 LLM；可选做后处理（例如背景清理、统一尺寸）。

### 3.2 工具需求

| 工具 | 用途 | 调用方式 |
|------|------|----------|
| Claude Code CLI | 意图路由/结构化输出(可选) | subprocess调用 `claude -p --output-format json` |
| Gemini Image API | 图像生成 | HTTP API |
| Streamlit | Web界面（当前为本地验证工作台） | 本地运行 |
| 本地文件系统 | 存储任务数据 | JSON文件 |

### 3.3 Prompt模板与美术规范接口设计

**需求整理Agent Prompt模板**
- 输入：资产类型、原始描述、分类、项目背景、额外上下文
- 输出：整理后的标题、描述、关键词、视觉重点
- 配置项：项目风格、规范约束

**出图指令生成Agent Prompt模板**
- 输入：整理后的标题、描述、关键词、视觉重点、游戏美术规范
- 输出：面向 Gemini Image API 的图像生成指令（prompt/约束/候选数量等）
- 配置项：色调规则、禁止元素、风格定义、构图约束（图标居中、留白、无文字等）

**说明：**
- 本 PRD 不固化具体的美术 Prompt 文案策略；美术 Prompt 会在项目推进过程中持续迭代。
- 本 PRD 只约束“美术规范在系统中的表达接口”，避免后续迭代 Prompt 时反复改动主流程和数据结构。

**建议的美术规范结构（示例）**
```json
{
  "style_preset_id": "default",
  "style_tags": ["fantasy", "energy glow", "high contrast"],
  "forbidden_elements": ["text", "watermark", "complex background"],
  "composition_rules": [
    "single centered subject",
    "clear silhouette",
    "sufficient negative space"
  ],
  "background_rules": {
    "mode": "simple_or_removable",
    "transparent_preferred": false
  },
  "post_process_rules": ["resize_512", "optional_bg_cleanup"]
}
```

### 3.4 Agent与编排层职责边界

| 模块 | 负责内容 | 不负责内容 |
|------|----------|------------|
| 主控编排层 | 状态机、任务目录、批量任务与 item 调度、重跑、回退、导出、指标、错误恢复 | 直接生成需求摘要/Prompt/图片 |
| 需求整理Agent | 生成标题、描述、关键词、视觉重点 | 修改任务状态、直接落盘控制逻辑 |
| 出图指令生成Agent | 生成图像 Prompt、约束、候选策略 | 直接调用 UI 或管理文件版本 |
| 聊天意图路由Agent | 把自然语言转成结构化 action | 执行真实业务动作 |
| 出图程序 | 调用 Gemini Image API、重试、落盘图片 | 决定产品流程与审核逻辑 |

### 3.5 受控聊天 action 协议

MVP 不要求自由聊天覆盖所有需求，而是先支持有限 action 集。聊天层的目标是把用户自然语言映射成结构化动作，再由编排层执行。

**结构化输出示例**
```json
{
  "action": "change_color",
  "target_step": "image_prompt",
  "params": {
    "primary_color": "blue"
  },
  "reason": "用户希望把主色调改成蓝色",
  "confidence": 0.91
}
```

**MVP 建议支持的 action**

| action | 含义 |
|--------|------|
| rerun_brief_generation | 重跑需求整理 |
| rerun_prompt | 重跑出图指令生成 |
| rerun_image | 使用当前 prompt 重跑出图 |
| edit_brief | 手动修改标题/描述/关键词/视觉重点 |
| edit_prompt | 手动修改 prompt 或约束 |
| change_color | 修改主色调/辅色 |
| change_element | 增加或移除视觉元素 |
| rollback_step | 回退到上一阶段版本 |
| approve_current_step | 审核通过当前步骤 |

**回退策略：**
- 如果聊天意图识别失败，系统不直接执行高风险动作，而是提示用户改用按钮或从候选 action 中选择。
- 任何通过聊天触发的动作，都必须写入事件日志，保证可回放。

### 3.6 评估策略

| 维度 | 评估方式 |
|------|----------|
| 需求整理准确性 | 用户审核通过率 |
| Prompt有效性 | 出图成功率、图片符合度 |
| 调度智能性 | 用户需要重跑的次数、是否需要手动修改 |
| 成本控制 | Token消耗、API调用次数 |

---

## 4. 技术规格

### 4.1 架构概述

```
┌─────────────────────────────────────────────────────────────────┐
│                Web界面层 (当前为 Streamlit 本地验证工作台)       │
│  ┌─────────────────┐    ┌───────────────────────────────────┐  │
│  │   任务工作区     │    │ 聊天/指令（按钮 + 自然语言）       │  │
│  │   (表单+审核)   │    │ (可选：Claude Code意图路由)        │  │
│  └─────────────────┘    └───────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    本地编排层 (Python)                           │
│  - 步骤执行/重跑/手改/落盘/导出                                  │
│  - (可选) claude -p --output-format json 做结构化输出/意图路由   │
│  - 调用 Gemini Image API 出图（批量/重试/并发控制）              │
└─────────────────────────────────────────────────────────────────┘
          ↓                                   ↓
┌───────────────────────────┐   ┌───────────────────────────────┐
│ Claude Code CLI (可选)     │   │ Gemini Image API               │
└───────────────────────────┘   └───────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                         数据层 (本地文件系统)                     │
│  projects/ai-icon-pipeline/tasks/task_XXX/                    │
│  ├── task.json      (批任务快照)                                │
│  ├── events.jsonl   (task级事件日志)                            │
│  ├── configs/       (风格与运行配置)                             │
│  └── items/         (各 item 的版本、图片、指标)                │
└─────────────────────────────────────────────────────────────────┘
```

**补充说明：**
- Streamlit 在当前阶段承担“本地验证工作台”的职责，主要用于快速打通流程、验证状态机、版本化、Provider 配置与 Agent 行为。
- 后续如果继续追求产品级交互（复杂导航、弹窗、强联动表格、富文本、多人协作等），建议保留现有 Python 核心层，并在其外增加 API 层与真正的 Web 前端，而不是继续把最终 UI 形态压在 Streamlit 上。

### 4.2 状态机与流程约束

系统采用显式状态机，而不是依赖 UI 推断当前进度。这样可以支持重跑、回退、恢复任务和回放历史。

| 状态 | 含义 | 允许动作 |
|------|------|----------|
| draft | item 刚创建，尚未执行 | generate_brief |
| brief_generated | 需求整理结果已生成，待审核 | approve_brief, rerun_brief, edit_brief |
| brief_approved | 需求整理已确认 | generate_prompt, rollback_to_brief |
| prompt_generated | 出图指令已生成，待审核 | approve_prompt, rerun_prompt, edit_prompt |
| prompt_approved | 出图指令已确认 | generate_image, rollback_to_prompt |
| image_generated | 图片已生成，待审核 | approve_image, rerun_image, rollback_to_prompt |
| completed | 当前 item 最终结果通过审核 | export_item, archive_task |
| failed | 当前 item 某一步失败，需要人工处理 | retry_step, rollback_step, archive_task |
| archived | 已归档 | view_only |

**状态机原则：**
- 每次动作都必须明确输入状态、输出状态和触发来源（按钮/聊天/系统自动）。
- 重跑不会覆盖历史版本，而是生成新的版本号。
- 回退动作只改变“当前采用版本”，不删除旧版本。

### 4.3 任务工作区结构

为支持回放、版本化和受控聊天，任务目录采用“当前状态 + 历史产物 + 事件日志”三层结构。

```
任务目录: tasks/task_{序号}/
├── task.json          # 批任务快照（item列表、汇总状态）
│   {
│     "task_id": "task_001",
│     "task_name": "三国奇幻首批图标",
│     "project_context": "三国奇幻",
│     "status": "in_progress",
│     "items": ["item_001", "item_002"],
│     "style_spec_ref": "configs/style_spec.json"
│   }
├── events.jsonl       # task级结构化事件日志（按钮/聊天/系统动作）
│   {"timestamp": "...", "source": "button", "action": "generate_brief", "item_id": "item_001", "from": "draft", "to": "brief_generated"}
│   {"timestamp": "...", "source": "chat", "action": "change_color", "target_step": "image_prompt", "result": "accepted"}
├── configs/
│   ├── style_spec.json      # 美术规范接口数据
│   └── runtime_config.json  # 模型、候选数、重试次数等运行配置
├── items/
│   ├── item_001/
│   │   ├── item.json
│   │   ├── metrics.json
│   │   ├── events.jsonl
│   │   ├── artifacts/
│   │   │   ├── brief_generation/
│   │   │   ├── image_prompt/
│   │   │   └── image_generation/
│   │   └── images/
│   └── item_002/
└── exports/
    └── task_bundle.zip
```

**数据结构原则：**
- `task.json` 记录批任务层面的事实和 item 列表，不承担 item 级历史归档职责。
- 所有 AI 输出都进入各 item 的 `artifacts/`，并保留版本号。
- task 级和 item 级操作都记录事件日志，保证可追溯性和回放。
- item 是最小执行单元；task 是批量调度和展示单元。

### 4.4 审核与回退规则

| 阶段 | 审核项 | 通过后动作 | 不通过后动作 |
|------|--------|------------|--------------|
| 需求整理审核 | 标题是否清晰、描述是否准确、关键词和视觉重点是否有效 | 锁定当前需求整理版本并进入 Prompt 生成 | 重跑或手动修改 |
| Prompt审核 | 是否包含主体、风格、构图、禁用元素、候选策略 | 锁定当前 Prompt 版本并进入出图 | 重跑、手改或回退需求整理 |
| 图片审核 | 主体是否清晰、构图是否适合作为图标、背景是否可控、是否符合预期风格 | 标记为 approved 并可导出 | 重跑图片或回退 Prompt |

### 4.5 集成点

| 组件 | 集成方式 | 关键参数 |
|------|----------|----------|
| Claude Code CLI | subprocess | claude -p --output-format json |
| Gemini Image API | HTTP REST | endpoint, model, auth_token |
| 本地存储 | 文件系统 | projects/ai-icon-pipeline/ |

### 4.6 安全与隐私

- **API密钥**：M4 阶段改为通过全局“设置”界面录入，并保存到本地忽略文件；不提交到 Git，不写入任务目录。长期可以进一步迁移到系统钥匙串或更安全的本机凭据存储。
- **任务数据**：仅本地存储，不上传到任何云服务
- **用户输入**：无敏感信息处理需求

### 4.7 Provider / 模型 / 设置系统（M4A）

M4 不再把“受控聊天”视为唯一重点，而是先补齐多 Provider 与模型配置骨架。这样后续不论是继续扩 Provider，还是迁移到正式前端，都不会反复重构主流程。

**第一阶段支持范围**

| 类别 | 支持 Provider | 说明 |
|------|---------------|------|
| 文本 LLM | Gemini, DeepSeek | 用于需求整理与出图指令生成 |
| 图像生成 | Gemini | 用于候选图生成 |

**模型来源**
- 用户在全局“设置”中录入 API Key。
- 系统通过 Provider 官方接口拉取可用模型列表，而不是在代码或文档中写死模型名。
- 模型下拉应按阶段过滤：
  - 需求整理：文本模型
  - 出图指令：文本模型
  - 候选图：图片模型

**配置继承规则**
1. 全局默认模型
2. 批次级覆盖
3. 条目阶段覆盖

即：`全局默认 -> 批次覆盖 -> 条目阶段覆盖`

**当前 UI 约束**
- Provider / API Key / 默认模型 属于全局设置，不与某个批次绑定。
- 批次级只负责项目背景、统一风格要求，以及对默认模型的批次覆盖。
- 条目级只负责当前条目在 `需求整理 / 出图指令 / 候选图` 三个阶段的局部覆盖。

---

## 5. 风险与路线图

### 5.1 分阶段里程碑

| 里程碑 | 目标 | 时间 | 交付物 | 验收标准 |
|--------|------|------|--------|----------|
| M0 | 设计定版 | 第1-2天 | PRD定稿、状态机、数据结构、action schema | 技术路线统一为 Gemini；开发可按文档开工 |
| M1 | CLI核心流水线 | 第1周 | 命令行任务创建、文案生成、Prompt生成、出图、落盘 | 不依赖 UI 可完整跑通单任务 |
| M2 | 版本化与审核能力 | 第2周 | 历史版本、事件日志、单步重跑、回退、人工修改 | 任一步骤都可回看历史并切换当前采用版本 |
| M3 | Streamlit工作台 | 第3周 | 表单输入、结果预览、按钮审核、任务恢复 | 非技术用户可以完成一次完整任务；UI 作为本地验证台可用 |
| M4A | Provider / 模型 / 设置系统 | 第4周前半 | 全局设置、本地 API Key 管理、模型列表同步、三层继承 | 用户可在页面配置 Gemini / DeepSeek，并为各阶段选择真实模型 |
| M4B | 真实生成链路接入 | 第4周后半 | Gemini / DeepSeek 文本调用、Gemini 图像调用、artifact 元数据 | 不依赖 mock 也能真实生成设计说明、出图指令与候选图 |
| M4C | 受控聊天Agent | 第5周前半 | 自然语言转结构化 action、失败回退按钮 | 聊天支持有限 action 集，解析失败不阻断流程 |
| M5 | 前后端衔接与正式前端基础盘 | 第5周后半 | 薄 API 层、正式前端骨架、迁移方案、核心流程接线 | 在不推翻核心流水线的前提下，正式 UI 可以开始替代 Streamlit 主工作流 |
| M6 | 正式前端产品化与交付形态 | vNext | 产品级交互打磨、异步状态体验、候选池体验、桌面壳/本地 Web 交付 | 美术/策划可以稳定使用正式 UI，不再依赖 Streamlit |

### 5.2 技术风险

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| Gemini出图一致性 | 风格漂移/构图不稳定 | 强化美术规范与结构化约束；提供风格预设；一次出多候选 |
| 透明背景/可用性 | UI图标难统一 | 增加后处理(可选)；或约束生成“可抠图背景”方案 |
| API限流/失败重试 | 批量出图卡住 | 并发控制、指数退避重试、可中断/续跑 |
| Claude Code调用成本 | 预算超支 | 只在需要结构化/意图路由时调用；本地控制预算 |
| Prompt效果不稳定 | 输出质量波动 | 沉淀Prompt模板库，版本管理与回归样例集 |
| 本地存储容量 | 历史数据堆积 | 定期清理/归档旧任务 |

### 5.3 迭代优化机制

- **用户反馈收集**：每次任务完成后收集满意度
- **Prompt版本管理**：记录每次Prompt修改，可回滚
- **指标监控**：每周review指标数据，优化Prompt配置

### 5.4 Streamlit 与后续前端迁移策略

- Streamlit 在 M3-M4 阶段被视为**内部验证工作台**，目标是验证流程、状态机、Provider、模型继承与受控聊天，而不是承担最终产品交互形态。
- 从 M4 开始，应尽量把业务逻辑继续沉淀到 Python 核心层（`pipeline / storage / settings / providers`），避免把关键逻辑堆在 UI 壳里。
- 当需要产品级交互时，优先采用：
  1. 保留现有 Python 核心层
  2. 增加轻量 API 层
  3. 迁移到真正的 Web 前端
- 该策略的目标是：**允许先用 Streamlit 验证，再换前端，而不留下不可控技术债。**

### 5.5 M5 执行重点

M5 不再被定义为“继续打磨 Streamlit Demo”，而是正式进入前后端衔接阶段。核心目标有三项：

1. **补薄 API 层**
   - 面向正式前端，而不是继续把核心交互压在 Streamlit rerun 模型上。
   - API 层优先包住当前最稳定的核心能力：批次、条目、阶段执行、版本、异步图片任务、Provider 配置。

2. **搭正式前端骨架**
   - 左侧批次导航
   - 批次详情 / 条目工作台
   - 设置弹窗 / 新建弹窗
   - 异步图片状态与候选池
   - 不要求第一版视觉完美，但要求交互模型正确且不被 Streamlit 的刷新机制打断。

3. **保持 CLI 与 core 继续可用**
   - CLI 继续作为外部 Agent 与自动化入口
   - core 继续作为真实业务逻辑层
   - Web UI 不得重复实现状态机与 Provider 逻辑

### 5.6 Git与版本管理策略

- 使用 Git 做阶段化版本管理，保证每个里程碑都可回退、可演示。
- `main` 分支始终保持“当前可演示”状态，不在 `main` 上直接堆叠未完成功能。
- 每个里程碑使用独立功能分支，例如 `codex/m1-cli-pipeline`、`codex/m4-chat-routing`。
- 每完成一个里程碑，打一个可回退的 tag，例如 `v0.1-cli`、`v0.2-review`、`v0.3-chat`。
- Prompt 模板、style spec、task/item schema 与代码一样纳入版本管理，避免“代码可回退、配置不可回退”。
- 保留一组固定样例任务作为回归测试集，用于比较不同版本的效果、稳定性和成本。

### 5.7 推荐执行顺序

1. 先完成 CLI 核心流水线，再做 Streamlit 包装层。
2. 先完成按钮式审核，再叠加受控聊天 action。
3. 先完成 Provider / 模型 / 设置系统，再接入真实生成调用。
4. 先实现数据结构和事件日志，再做指标面板和导出。
5. 先保证主流程稳定，再逐步增强美术 Prompt 和风格预设。
6. 在 M4 完成后，M5 优先投入 API 层与正式前端基础盘，不再继续重押 Streamlit 交互打磨。

---

## 6. 附录

### 6.1 术语表

| 术语 | 定义 |
|------|------|
| Agent | AI Agent，能够理解指令、执行任务、返回结果的智能体 |
| Human-in-the-Loop | 人在回路，AI执行过程中有人工审核干预 |
| Meta-Agent | 元Agent，能够分析和优化其他Agent配置的Agent |
| Prompt | 给AI的指令文本 |
| Token | AI API计费单位 |

### 6.2 参考资料

- Claude Code Documentation (`claude --help`)
- Gemini API Documentation
- Streamlit Documentation

---

**文档版本**: v1.1
**创建日期**: 2024-03-15
**最后更新**: 2026-03-18
**状态**: 草稿
